{
  "name": "lightsup",
  "private": true,
  "type": "module",
  "description": "Simple theatrical lighting drafting program (static browser app)",
  "scripts": {
    "start": "python3 -m http.server 8080 --directory src",
    "test": "node --test tests/",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
